#include <iostream>
using namespace std;
int main() {
    int n;
    cout<<"Enter the number of elements: ";
    cin>>n;
    int a[n], sum = 0;
    for (int i=0; i<n;i++) {
        cin>>a[i];
        sum += a[i];
    }
    cout<<"The sum is: "<<sum;
    return 0;
}
